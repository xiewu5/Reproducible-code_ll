# 读取 GEMMA 结果文件


gwas <- read.table(
  "",
  header = TRUE,
  stringsAsFactors = FALSE
)
# 提取 P 值
p <- gwas$p_wald

# 基本清洗
p <- p[!is.na(p) & p > 0 & p <= 1]

# 转换为卡方统计量
chisq <- qchisq(1 - p, df = 1)

# 计算 λ
lambda <- median(chisq) / qchisq(0.5, df = 1)

lambda

